# セットアップ手順書

はじめから順番にやれば動くように書いています。所要時間は **30〜40分** くらいです。
専門知識は不要ですが、GitHub のアカウント作成だけ済ませてから始めてください。

**全体の流れ**

1. [GitHub リポジトリを作る](#1-github-リポジトリを作る)（10分）
2. [ファイルをアップロードする](#2-ファイルをアップロードする)（5分）
3. [Discord の通知先を用意する](#3-discord-の通知先を用意する)（10分）
4. [Webhook URL を GitHub に登録する](#4-webhook-url-を-github-に登録する)（3分）
5. [初回実行して動作を確認する](#5-初回実行して動作を確認する)（5分）
6. [Google カレンダーに登録する](#6-google-カレンダーに登録する)（5分）
7. [うまくいかないとき](#7-うまくいかないとき)
8. [この先：一覧と検索を使う](#8-この先一覧と検索を使う)

---

## 事前に知っておいてほしいこと

### リポジトリは「パブリック（公開）」にします

Google カレンダーの「URLで追加」機能は、**誰でもアクセスできるURL**しか受け付けません。
プライベートリポジトリのファイルURLには期限付きのトークンが必要で、Googleカレンダーからは
購読できないため、**パブリック**にする必要があります。

公開されるのは次のものだけです。

- このプログラムのソースコード
- Tonamel に載っている大会情報（もともと公開情報です）
- 生成されたカレンダーファイル

**Discord の Webhook URL は公開されません。** GitHub の「Secrets」という暗号化された場所に
保存し、実行ログにも出ないようになっています。

もし公開したくない場合は、Discord 通知だけを使い、カレンダー購読をあきらめる（＝プライベート
リポジトリにする）という選択もできます。その場合は手順6を飛ばしてください。

### 夜中は通知が来ません

**23時〜翌8時（日本時間）は Discord へ通知しません。**
ただし裏では通常どおり動いていて、カレンダーも夜のうちに更新されます。
夜に見つかった大会は、**朝8時10分ごろの実行でまとめて1通届きます。**

時間帯は後から変えられます（[通知の時間帯を変えたい](#通知の時間帯を変えたい)）。

### GitHub Actions の無料枠について

パブリックリポジトリでの GitHub Actions は**実行時間が無制限で無料**です。
3時間おき（1日8回）の実行でも費用はかかりません。

---

## 1. GitHub リポジトリを作る

1. <https://github.com> にログインします（アカウントが無ければ「Sign up」から作成）。
2. 画面右上の **＋** ボタン → **New repository** をクリック。
3. 次のように入力します。

   | 項目 | 入力内容 |
   |---|---|
   | Repository name | `xrossstars-tonamel-watcher`（好きな名前でOK） |
   | Description | `Tonamelの Xrossstars 大会を監視する`（任意） |
   | 公開設定 | **Public** を選択 ← 重要 |
   | Add a README file | **チェックを外す**（zipに入っています） |
   | Add .gitignore | **None** のまま |
   | Choose a license | **None** のまま |

4. **Create repository** をクリック。

これで空のリポジトリができ、「…or upload an existing file」というリンクがある画面が出ます。

---

## 2. ファイルをアップロードする

お手元の `xrossstars-tonamel-watcher.zip` を**解凍**しておいてください。
解凍すると `xrossstars-tonamel-watcher` フォルダの中に `src` や `.github` などが入っています。

### 方法A：ブラウザからドラッグ＆ドロップ（おすすめ・Git不要）

1. さきほどの画面の **uploading an existing file** をクリック
   （すでに閉じてしまった場合は、リポジトリのトップ → **Add file** → **Upload files**）。
2. 解凍したフォルダを開き、**中身のファイル・フォルダをすべて選択**して、
   ブラウザの点線枠の中へドラッグ＆ドロップします。
   - ⚠️ `xrossstars-tonamel-watcher` フォルダ**ごと**ではなく、**中身**を入れてください。
   - ⚠️ **`.github` フォルダを入れ忘れないでください。** これが無いと自動実行されません。
     Mac の Finder や Windows のエクスプローラーでは、ドットで始まるフォルダが
     隠れていることがあります。表示方法は下の「隠しフォルダの出し方」を参照してください。
3. 下の「Commit changes」欄はそのままで **Commit changes** をクリック。
4. アップロード後、リポジトリのトップ画面に次のものが並んでいれば成功です。

   ```
   .github/    data/    src/    tests/    debug/
   .gitignore  README.md  SETUP.md  requirements.txt
   ```

   `.github` が見当たらない場合は、もう一度 **Add file → Upload files** から
   `.github` フォルダだけドラッグしてください。

<details>
<summary>隠しフォルダの出し方</summary>

- **Mac (Finder)**: 対象フォルダを開いた状態で `command` + `shift` + `.`（ピリオド）
- **Windows (エクスプローラー)**: 「表示」タブ →「隠しファイル」にチェック

</details>

### 方法B：git コマンドを使う

```bash
cd xrossstars-tonamel-watcher
git init
git add -A
git commit -m "初期コミット"
git branch -M main
git remote add origin https://github.com/<あなたのユーザー名>/xrossstars-tonamel-watcher.git
git push -u origin main
```

---

## 3. Discord の通知先を用意する

通知を受け取る Discord サーバーとチャンネルを用意します。
**自分専用のサーバーを新しく作るのがおすすめ**です（他人に見られず、通知も混ざりません）。

### 3-1. サーバーを作る（すでにある場合は飛ばしてOK）

1. Discord を開き、左端のサーバー一覧いちばん下の **＋（サーバーを追加）** をクリック。
2. **オリジナルの作成** → **自分と友達のため** を選択。
3. サーバー名を入力（例：`大会情報`）して **作成**。

### 3-2. 通知用チャンネルを作る

1. 左のチャンネル一覧の「テキストチャンネル」の横の **＋** をクリック。
2. チャンネル名を入力（例：`xrossstars-大会`）して **チャンネルを作成**。

### 3-3. Webhook を発行する

1. 作ったチャンネルの名前にマウスを乗せ、右側に出る **⚙️（チャンネルの編集）** をクリック。
   - スマホの場合：チャンネル名を長押し → **設定** / **チャンネルを編集**
2. 左メニューの **連携サービス**（Integrations）をクリック。
3. **ウェブフック**（Webhooks）→ **新しいウェブフック**（New Webhook）をクリック。
4. 自動で「Spidey Bot」などの名前で作られます。名前は
   **`Tonamelウォッチャー`** などに変えておくと分かりやすいです。
   アイコンも好きな画像に変えられます。
5. **ウェブフックURLをコピー**（Copy Webhook URL）をクリック。
6. **変更を保存** をクリック。

コピーされた URL は次のような形をしています。

```
https://discord.com/api/webhooks/1234567890123456789/AbCdEf...（長い文字列）
```

> ⚠️ **この URL は絶対に他人に見せないでください。** 知っている人は誰でも、あなたの
> チャンネルにメッセージを投稿できてしまいます。GitHub のコード内に直接書き込むのも
> 絶対にやめてください（次の手順の Secrets を使います）。
> もし漏らしてしまったら、同じ画面の「ウェブフックを削除」で消して作り直せば無効になります。

---

## 4. Webhook URL を GitHub に登録する

1. GitHub のリポジトリを開きます。
2. 上部タブの **Settings**（設定）をクリック。
   - 見当たらない場合は「…」メニューの中にあります。
3. 左メニューの **Secrets and variables** → **Actions** をクリック。
4. 緑色の **New repository secret** ボタンをクリック。
5. 次のように入力します。

   | 項目 | 入力内容 |
   |---|---|
   | Name | `DISCORD_WEBHOOK_URL` ← **1文字も違わないように** |
   | Secret | さきほどコピーした Webhook URL を貼り付け |

6. **Add secret** をクリック。

登録すると値は二度と表示されません（`***` と表示されます）。それで正常です。
間違えた場合は、鉛筆アイコンから上書きできます。

---

## 5. 初回実行して動作を確認する

自動実行を待たずに、手動で1回動かして確認します。

1. リポジトリの上部タブ **Actions** をクリック。
2. 初回は「Workflows aren't being run on this forked repository」や
   「I understand my workflows, go ahead and enable them」といった確認が出ることがあります。
   出たら緑のボタンを押して有効化してください。
3. 左側の **Watch Tonamel (Xrossstars)** をクリック。
4. 右側の **Run workflow** ボタン → もう一度 **Run workflow**（緑のボタン）をクリック。
   - オプションはすべて既定（オフ）のままでOKです。
   - 手動実行では**夜中でもすぐ通知が届きます**（動作確認のためです）。
     夜間の保留動作そのものを試したいときだけ `respect_quiet` にチェックを入れてください。
5. 少し待つと一覧に実行が現れます。クリックすると進行状況が見られます。
   **5〜10分ほどかかります**（Chromium のインストールに時間がかかるためです）。

### 確認すること

**① 実行が緑のチェック ✅ になっているか**

実行画面の上部に結果のサマリが出ます。こんな表示になっていれば成功です。

```
## Xrossstars 大会ウォッチャー
- 一覧取得: 12件 / API由来: 12件
- 新規: 12件（初回シード）
- 変更: 0件
- DB総件数: 12 / ICSイベント: 11
```

**② Discord に投稿されているか**

初回は個別通知ではなく、次のようなまとめが1通だけ届きます。

> ✅ **Xrossstars大会ウォッチャーを開始しました**
> 現在Tonamelに掲載中の大会 **12件** を初期登録しました。次回以降、新しく掲載された大会だけをお知らせします。

**③ ファイルが更新されているか**

リポジトリのトップに戻り、`data` フォルダを開いて、

- `known_competitions.json` の中に大会が入っている
- `calendar.ics` ができている

ことを確認してください。「大会を1件も取得できませんでした」となった場合は
[7. うまくいかないとき](#7-うまくいかないとき) を見てください。

以降は **3時間おきに自動で実行されます**。何もしなくて大丈夫です。

---

## 6. Google カレンダーに登録する

### 6-1. カレンダーのURLを取得する

1. リポジトリのトップ → `data` フォルダ → **`calendar.ics`** をクリック。
2. 画面右上の **Raw** ボタンをクリック（または「Download」の隣）。
3. ブラウザのアドレスバーに出ている URL をコピーします。次の形になっているはずです。

   ```
   https://raw.githubusercontent.com/<ユーザー名>/xrossstars-tonamel-watcher/main/data/calendar.ics
   ```

   > ⚠️ `https://github.com/...` で始まるURLでは**登録できません**。
   > 必ず **`raw.githubusercontent.com`** で始まるURLを使ってください。
   > 手打ちする場合は、上の形の `<ユーザー名>` を置き換えれば同じものになります。

### 6-2. Google カレンダーに追加する

**パソコンのブラウザから行ってください。スマホアプリからは登録できません**
（一度登録すれば、スマホアプリにも自動で同期されて表示されます）。

1. <https://calendar.google.com> を開きます。
2. 左側の **「他のカレンダー」** の横にある **＋** をクリック。
3. **「URLで追加」** を選択。
4. さきほどの `raw.githubusercontent.com/...` の URL を貼り付けます。
5. **「カレンダーを追加」** をクリック。

左側の一覧に `Xrossstars 大会 (Tonamel)` が追加され、大会の予定が表示されれば完成です。
表示名や色は、カレンダー名の横の「⋮」→「設定」から変更できます。

### 6-3. 知っておくべき制限

- **Google 側の更新は即時ではありません。** 数時間〜1日程度の遅れが出ることがあり、
  この間隔は Google が決めているためこちらからは変更できません。
  **早く知りたいものは Discord 通知の側で受け取る**、という使い分けが前提の設計です。
- 予定は「予定なし（透明）」として登録されるので、他の予定と重なっても邪魔になりません。
- 開催時刻が取れなかった大会は「終日の予定」として表示されます。

---

## 7. うまくいかないとき

### 「大会を1件も取得できませんでした」と出る／実行が赤い ❌ になる

Tonamel 側のページ構造が変わった可能性が高いです。原因を特定するための材料が自動で残ります。

1. **Actions** → 失敗した実行をクリック。
2. ページ下部の **Artifacts** の欄にある `debug-〇〇` をダウンロード（zip）。
3. 中身:

   | ファイル | 内容 |
   |---|---|
   | `list.png` | そのときブラウザに実際に表示されていた画面 |
   | `list.html` | 描画後のHTML |
   | `list_api_urls.txt` | ページが裏で叩いていたAPIのURL一覧 |
   | `list_api_payloads.json` | そのAPIが返したJSON |

   `list.png` を見れば、「そもそも大会が0件だった」のか「ページ構造が変わった」のか
   「アクセスがブロックされた」のかが一目で分かります。

4. 直すときは `src/scrape.py` の `JS_COLLECT_CARDS`（カードを拾うJS）と
   `_LABELS`（ラベル文言）を調整します。**この2箇所以外はほぼ触らずに済むよう作ってあります。**

デバッグ出力は、手動実行時に **Run workflow** の `debug` にチェックを入れれば、
成功時でも取得できます。

### Discord に何も来ない

順に確認してください。

1. **新着が無いだけの可能性が高いです。** 実行ログに
   `[discord] 新着・変更なしのため通知しません` と出ていれば正常です。
1. **夜中（23時〜8時）は通知しません。** ログに
   `[discord] 静音時間帯（23時〜8時 (JST)）のため通知しません。保留中: 新着2件`
   と出ていれば正常で、朝8時台の実行でまとめて届きます。
2. ログに `DISCORD_WEBHOOK_URL が未設定のため通知をスキップしました` と出ている
   → 手順4の Secret 名が `DISCORD_WEBHOOK_URL` と完全に一致しているか確認してください。
3. ログに `[discord] HTTPエラー 401` / `404` が出ている
   → Webhook が削除されているか、URLが間違っています。手順3-3から作り直してください。

### Google カレンダーに反映されない

- URL が `raw.githubusercontent.com` で始まっているか確認してください。
- GitHub 側にキャッシュがあるため、`calendar.ics` の更新が URL に反映されるまで
  数分かかります。さらに Google 側の取得間隔があるので、**丸1日待ってから**判断してください。
- それでも来ない場合は、Google カレンダーで一度そのカレンダーを削除し、登録し直してください。

### 自動実行が止まってしまった

GitHub は、**60日間まったく更新がないリポジトリのスケジュール実行を自動停止します**。
このシステムは大会が増えるたびに自動コミットするので通常は止まりませんが、
大会が長期間追加されないと止まる可能性があります。

- 停止前に GitHub からメールが届きます。
- **Actions** 画面に「このワークフローは無効化されています」と出たら、
  **Enable workflow** を押せば再開します。
- 予防したい場合は、たまに README を1文字直してコミットするだけで十分です。

また、GitHub Actions のスケジュール実行は**混雑時に数分〜数十分遅れることがあります**
（GitHub の仕様です）。3時間おきの用途では実害はありません。

### 通知の時間帯を変えたい

`.github/workflows/watch.yml` の `env:` の中を書き換えます（GitHub のWeb画面から
鉛筆アイコンで直接編集できます）。**時刻は日本時間の「時」で書いてください。**

```yaml
      QUIET_HOURS_ENABLED: "1"
      QUIET_HOURS_START: "23"   # ← ここから静かにする
      QUIET_HOURS_END: "8"      # ← ここで解除。この時刻以降の実行でまとめて届く
```

| したいこと | 書き換える内容 |
|---|---|
| 0時〜7時だけ黙らせる | `START: "0"` / `END: "7"` |
| 22時〜9時に広げる | `START: "22"` / `END: "9"` |
| 24時間いつでも通知する | `QUIET_HOURS_ENABLED: "0"` |

**静音を解除する時刻を変えたら、その直後に実行が来るよう cron も直してください。**
たとえば7時解除なら、日本時間7時台に実行がある並びにします。

```yaml
    - cron: "10 2,5,8,11,14,17,20,23 * * *"   # ← ここ（UTC指定）
```

| したいこと | 書き換える内容 |
|---|---|
| 朝7時10分から3時間おき | `"10 1,4,7,10,13,16,19,22 * * *"` |
| 6時間おき（朝8時10分始まり） | `"10 5,11,17,23 * * *"` |
| 1日1回だけ（朝8時10分） | `"10 23 * * *"` |

> cron は **UTC（日本時間 −9時間）** で書きます。日本時間の8時10分は `10 23`（前日23時10分）です。

### 通知が多すぎる／少なすぎる

- **回数を減らしたい** → 上の cron を6時間おきなどに変更してください。
- **日時変更のお知らせが要らない**（新着だけでいい）→ `watch.yml` の `env:` に次を足します。

  ```yaml
        NOTIFY_ON_CHANGE: "0"
  ```

### 全部やり直したい

`data/known_competitions.json` の中身を次に戻して（GitHub のWeb画面から鉛筆アイコンで編集できます）、
手動実行すれば初期状態からやり直せます。

```json
{
  "meta": { "schema_version": 1 },
  "competitions": {}
}
```

---

## 8. この先：一覧と検索を使う

ここまでで「新着がDiscordに届く」「カレンダーに並ぶ」状態になりました。
さらに、**集めた大会を一覧で見たり、条件で検索したり**できます。

- **Webページ**（スマホのブラウザで一覧・検索。追加のアカウント不要・5分）
- **Discord bot**（Discord内で `/一覧` `/検索`。Cloudflareアカウントが必要・40分）

手順は **[`BOT_SETUP.md`](BOT_SETUP.md)** にまとめてあります。
Webページだけでも十分使えるので、まずそちらだけ有効にするのがおすすめです。

なお、この2つを使わなくても通知とカレンダーは動き続けます。

---

## 補足：この仕組みが見ているもの

- 監視対象は **`https://tonamel.com/competitions?game=XrossStars&region=JP`** の1ページのみです。
- したがって、**Tonamel に登録されていない大会（X での告知のみ、公式サイトのみ、など）は
  検知できません。** これは当初の取り決めどおり、意図的にスコープ外としています。
- 大会情報は自動抽出のため、日時や会場をまれに取り違える可能性があります。
  参加を決める前に必ず Tonamel の大会ページ本体をご確認ください。
